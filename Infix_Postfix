#include <bits/stdc++.h>

using namespace std;

bool is_OP(char op){
    
    return op == '+' || op == '-' || op == '*' || op == '/';
}

template<typename x>
bool digit(x d){
    return isdigit(d) ? true : false;
}

int main() {

    stack <string> s;
    string a;
    getline(cin,a);
    // 12*5+7-(23+2)/5-4
    // 12 5 * 7 + 23 2 + 5 / - 4 -
    unsigned int len = a.length();
//     while(){

//     }
    cout << a;
    
    
    
    return 0;
}
