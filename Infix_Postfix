#include <iostream>
#include <stack>
#include <string>

using namespace std;

int precedence(char c){
    if (c == '/' || c == '*') return 2;
    else if (c =='+' || c == '-') return 1;
    else return 0;
}

bool IsOP(char c){
    return isdigit(c);
}

string infixToPostfix(string s){
    stack<char> st;
    string postFix;
}


int main(){

    string input;
    getline(cin,input);
    
    

return 0;
}
